import React from 'react';
import styles from './Header.module.css';

const Header = () => (
  <header className={styles.Header}>
    <h1 className={styles.Title}>CaesarCipher</h1>
  </header>
);

export default Header;
